<header>
	<div class="container clearfix">
		<!-- логотип -->
		<a href="{{ path('main') }}" class="logo">Yellow Duck</a>
		<!-- меню -->
		<nav>
			<ul>
				<li><a href="{{ path('info')}}">О Компании</a></li>
				<li><a href="{{ path('show_category', {id: 1}) }}">Каталог</a></li>
				<li><a href="{{ path('delivery') }}">Доставка и оплата</a></li>
				<li><a href="{{ path('contacts') }}">Контакты</a></li>
				<li><a href="{{ path('cart', {in_cart: in_cart}) }}">{% if in_cart == 0 %}Корзина{% else %}В корзине: {{ in_cart }}{% endif %}</a></li>
			</ul>
		</nav>
	</div>
</header>